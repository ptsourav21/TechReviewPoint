@model TechReviewPoint.Models.WishList
@{
    ViewBag.Title = "wishing";
    //Layout = "~/Views/Shared/ProductDetailsLayout.cshtml";
}

<h2>wishing</h2>
@**
@foreach (var item in Model)
{
    <tr style="text-align:center">
        <td>
            @Html.DisplayFor(modelItem => item.WishDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.UserName)
        </td>
    </tr>
}
*@
@Html.DisplayFor(model => model.WishDate)
